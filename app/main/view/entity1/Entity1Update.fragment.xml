<core:FragmentDefinition 
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">
    <VBox class="sapUiSmallMargin">
        <f:SimpleForm>
            <f:content>
                <Label text="Title" />
                <Input value="{local>/entity1/title}" />
                <Label text="Description" />
                <Input value="{local>/entity1/descr}" />
            </f:content>
        </f:SimpleForm>
    </VBox>
</core:FragmentDefinition>